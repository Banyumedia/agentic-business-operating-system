# UR-00  Convergence & Final QA Baseline

Tanggal: 2026-09-21. Executor: Hermes (agentic-bos-coding-agent).

## Base & Candidate

- Base snapshot plan: `750fd31` (stale  actual HEAD saat eksekusi lebih maju).
- Candidate HEAD final: **`9e0364b`** (main; termasuk MQ-01C6 remediasi QA findings).
- Runtime `main` worktree `D:\PROJECTS\agentic-bos`; hanya fixture runtime `storage/app/json/1/workflow_log.json` kotor (dipulihkan nilai default, bukan file asing).

## Foreign Writer Audit (fresh, sebelum audit lane)

- `git worktree list --porcelain`: 33 worktree terdaftar; tidak ada writer aktif menunjuk `agentic-bos` selain dev server `php artisan serve` 8003 (main) dan 8002 (legacy).
- Proses: codex/claude/opencode/kiro hidup di OS tapi TIDAK ada yang mengarah ke worktree agentic-bos (command line dicek, tanpa prompt sensitif).
- `git reflog -10`: hanya jejak MQ-01 orchestrator sendiri. Tidak ada aktivitas asing di main.

## Verifikasi fresh (dijalankan sendiri, bukan laporan delegasi)

```text
php vendor/phpunit/phpunit/phpunit  -> 856 passed, 4.376 assertions, 0 failure (3 notice pre-existing)
php vendor/laravel/pint/builds/pint --test -> PASS
npm run build -> built in 1.57s (app-BvRk9kiK.js / app-BUO41I-9.css)
smoke browser C5 (login nyata, 360x390 + 1280x800, keyboard, reload) -> TEMUAN 0
php artisan migrate (dev sqlite) -> expires_at migration DONE 15.01ms
```

## QA Independen MQ-01 (HEAD kandidat 9e0364b, termasuk delta C6)

Reviewer: OpenCode 1.18.31 read-only (`--agent plan`), output penuh di
`cache/opencode-qa-ur00-report.txt`. **Verdict: LAYAK.**

Checklist UR-00 tercakup:

| Item | Hasil |
|---|---|
| Tenant isolation & Livewire middleware fail-closed | PASS (`AppServiceProvider:50-53` persistent middleware intact; `EloquentCompanyContext:108-114` + `EnsureCompanyAccess:51-56` expired/null impersonation ditolak) |
| Exact money/decimal values | PASS (`CashFlowCalculator` untouched by C6; DECIMAL(18,2) + overflow + cents-regex test tetap hijau) |
| Widget-capability contract fail-closed | PASS (single-source `WidgetCapabilityMap`; `WIDGETS` const terhapus, 0 referensi tersisa) |
| Company display name parity JSON/Eloquent | PASS (fallback slug; banner pakai kontrak) |
| Mobile layout, keyboard, focus, a11y | PASS (dashboard.blade.php tidak diubah C6; smoke browser 0 temuan) |
| Rendered-page smoke | PASS (Playwright login nyata, 0 temuan) |

Temuan QA (semua LOW/INFO, non-blocking; dicatat, tidak menutup UR-00):

- QA-C6-1 LOW: audit-stamp `Company.php:23`/`ModuleSetting.php:14` tidak filter sesi impersonasi expired (attribution drift saja, bukan akses).
- QA-C6-2 LOW: tidak ada test eksplisit `expires_at = null` (tertutup oleh `whereNotNull`, untested).
- QA-C6-3 LOW: displayName trim-parity JSON vs Eloquent (whitespace-only JSON name lolos tanpa trim).
- QA-C6-4 INFO: sesi impersonasi expired meninggalkan `users.current_company_id` sampai admin menekan "Akhiri Sesi" (403 tetap benar).

## Audit Lane BI-A / BI-B (untrusted handoff)

### BI-A  `task/bi-a` @ `9d032af`

- **Sudah merged ke main** jauh sebelum UR-00 (ada di `git log main`; jadi bagian baseline yang di-QA di atas), bukan kandidat merge baru.
- Scope commit: `app/Services/Analytics/BusinessHealthAnalyzer.php` (199 baris), `config/analytics.php`, 7 test termasuk tenant isolation negatif. D-26 via `EntityRepository::for()`.
- Worktree bi-a: 2 commit di atas base lama (`0cf9636` paywall a11y fix, `47b753d` w2 refactor)  keduanya juga sudah ada di main.
- Status: **TIDAK ADA ACTION (baseline)**. Catatan: worktree `agentic-bos-bi-a` dirty `package-lock.json` + fixture runtime  tidak digabung, tidak disentuh.

### BI-B  `task/bi-b` @ `49bcaed`  **verdict: PARKIR**

- Belum merge. 2 commit: panel Kesehatan Usaha di dashboard (`f27d130`) + 5 test (`49bcaed`). 3 file: `app/Livewire/Dashboard.php`, `resources/views/livewire/dashboard.blade.php`, `tests/Feature/DashboardHealthPanelTest.php` (268 baris, termasuk owner-only negative + fail-closed analyzer + reload).
- **Konflik merge nyata dengan MQ-01**: `git merge --no-commit task/bi-b` menghasilkan CONFLICT di `app/Livewire/Dashboard.php`. BI-B dibangun pada base pra-MQ-01 (mount masih pakai `CompanyRoleResolver` + `BusinessHealthAnalyzer` injection; main MQ-01C2 sudah mengubah mount jadi `mount(DashboardComposer $composer)` + perbaikan error/reload state). `dashboard.blade.php` auto-merge tapi tumpang tindih zona render error/warning state C1.
- Kualitas intrinsik oke (owner-only D-50, fail-closed analyzer, uang pakai data nyata)  tapi rebase wajib, bukan merge langsung.
- BI adalah optional enhancement, bukan launch blocker. **PARKIR**: kandidat task `BI-B2` rebase + adaptasi kontrak MQ-01 (mount signature, loadError state, displayName) + QA ulang. Jangan merge sekarang.

## Verdict lane

| Branch | Verdict | Alasan |
|---|---|---|
| `task/bi-a` @ 9d032af | N/A  baseline | sudah merged & ter-QA sebagai bagian main |
| `task/bi-b` @ 49bcaed | **PARKIR** | konflik dengan MQ-01 di Dashboard.php; perlu rebase + adaptasi kontrak; bukan launch blocker |

## Risiko tersisa

1. QA-C6-1/2/3 (LOW)  catatan perbaikan kecil, tidak menghalangi UR-01.
2. Snapshot plan stale: runtime produksi (8010, Basic Auth 401, MySQL kosong, queue/scheduler belum diverifikasi) belum dicek ulang di eksekusi ini  harus diverifikasi fresh di UR-02.
3. `users.current_company_id` saat impersonation expired (QA-C6-4)  benign; UX "Akhiri Sesi" tetap benar.

## Merge / Final Gate

Tidak ada kandidat merge baru yang lolos ke Final Gate di UR-00 (BI-B diparkir).
Writer aktif di `main`: tidak ada. `main` = `9e0364b` diverifikasi hijau penuh.

## Next state

- **UR-00: DONE.**
- UR-01 `READY` di plan, tapi eksekusi tertahan gate `HUMAN:DEPLOY` + `HUMAN:SECRET`. Bagian pra-approval yang boleh (audit model/route/authorization, desain command provisioning idempoten + uji lokal, negative test non-admin 403) bisa dikerjakan tanpa approval.
- UR-02 bagian local implementation juga boleh pra-gate.
