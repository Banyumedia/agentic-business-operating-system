# DECISIONS LOG — KONSOLIDASI DISKUSI PRD AGENTIC BOS
## Single Source of Truth Seluruh Keputusan Diskusi

**Tanggal konsolidasi:** 2026-09-12 (revisi 2026-09-16)
**Sumber:** Diskusi PRD + dokumen modul warisan ERP Prime (tidak ada di repo ini; hanya keputusannya yang dibawa)
**Lokasi dokumen:** `D:\PROJECTS\agentic-bos\docs\`

Status: `LOCKED` = keputusan final Bos. `OPEN` = menunggu diskusi lanjutan.

---

## Keputusan LOCKED

| ID | Keputusan | Detail |
|---|---|---|
| D-01 | **6 Preset bisnis awal, arsitektur untuk ≥50** | Agency, F&B, Apotek, Event Organizer, Kontraktor, Persewaan sebagai **preset pertama**, bukan batas. Onboarding 1-klik + override per company. Matriks di `INDUSTRY_PRESETS.md`. Sejak D-31, preset adalah **data** sehingga preset ke-7..50 ditambah tanpa kode. |
| D-02 | **Model config = 1B (Preset + Override)** | Bukan toggle manual murni. Company pilih preset industri → `definition` preset dimuat (kapabilitas, terminologi, workflow, widget) → bisa override via `module_settings` (Pengaturan > Fitur Bisnis). |
| D-03 | **Tax mode per Business Identity** | Semua identity bisa pilih: jenis buku `taxable` / `non_taxable`. Jika taxable → pilihan `price_includes_tax` (harga sudah termasuk PPN atau belum). Ini adalah konfigurasi dasar semua transaksi; formula DPP inclusive = total / 1.11. Di `REQUIREMENTS.md` §1. |
| D-04 | **NalarPesan ≠ Agentic BOS — dua layanan berbeda** | NalarPesan = Mini POS front-facing, tujuan utama memudahkan klien order + pemilik kelola bisnis, nilai plus CRM WhatsApp (pesan via WA; opsi channel lain tetap ada). BOS = back-office ERP + Karyawan AI. Terhubung via webhook bridge idempotent. |
| D-05 | **Membership & pricing DINAMIS, tidak hardcode** | Harga, jumlah paket, kuota ditentukan belakangan via riset pasar. Simulasi 3 tier di `COMMERCIAL_AND_AI_AGENTIC_SPEC.md` hanya placeholder, edit manual via Super Admin. Struktur canonical: `membership_plans`, `company_memberships`, dan `token_ledger_entries`. |
| D-06 | **Arsitektur Multi-Node dan Tenant Hybrid untuk 500+ klien** | SaaS reguler memakai database bersama dengan isolasi `company_id` yang fail-closed; Enterprise dapat memakai database/VPS dedicated melalui `TenantProvisioner`. 1 Node Hermes (Process Manager) maksimal memegang ~100 sesi Klien. Laravel menyimpan *Node Registry* dan melakukan *Load Balancing* ke Node yang masih kosong. (Mencegah Single Point of Failure akibat *Memory Leak*). |
| D-07 | **Model AI per-tenant, masing-masing pilih sendiri** | Tenant dapat memilih model/provider sendiri dari katalog yang disediakan platform. |
| D-08 | **Topup token otomatis via QRIS** | Pembayaran QRIS → webhook callback payment gateway → kuota token ter-update otomatis tanpa konfirmasi admin. Webhook wajib fail-closed (validasi signature, idempotency, transaksi atomik). **Belum ada implementasi di repo ini** — dibangun pada T-19. |
| D-09 | **White-label 100% + anti-jailbreak** | Klien tidak bisa edit raw SOUL.md/tools. SOP via form terstruktur (max ~300 char) → service rakit jadi SOUL.md baku. Toolset tenant whitelist: hanya MCP ERP; terminal/file/code-exec OFF. Hilangkan semua jejak Hermes/Nous dari respons. |
| D-10 | **Role grup WA + kuota per paket** | Grup dikunci role: Kasir (dilarang lihat laba-rugi/gaji), Gudang (dilarang kasir/keuangan), Keuangan. Kuota `max_wa_groups` dari membership plan. Bind `group_jid` permanen. Onboarding `/role` command di grup. |
| D-11 | **DM Bos = Personal Assistant** | Nomor owner verified → akses full read data keuangan tenant (omzet, cashflow, margin), analisis bebas konteks, aksi finansial wajib 2-step confirmation ("YA 1234"). |
| D-12 | **Sidebar & form dinamis (zero bloat)** | Menu level 1/2 dari `DynamicMenuRegistry` — fitur mati tidak dirender sama sekali. Route di-guard middleware `EnsureFeatureEnabled` (403). Form field kondisional per feature flag (contoh: PIC hanya Agency/EO/Kontraktor). |
| D-13 | **Deployment NalarPesan = Hybrid Sharded** | Fase 1 (0–50 klien) 1–2 shard shared + route key `tenant_slug→shard_id` di DB sejak awal. Fase 2 scale-out tambah node (1 shard = 25–50 klien, node 8–16GB RAM). Nomor Official Meta Cloud API = jalur terpusat (bebas ban-risk). Klien spam-problem/high-volume → eject dedicated VPS. |
| D-14 | **Token AI disediakan platform (key pusat)** | Tenant pilih model dari katalog platform, memakai key pusat Nalarin. Biaya model dihitung ke metering token per tenant (bukan BYO-key). Margin dari selisih harga token. |
| D-15 | **Semua tenant bebas pilih model premium** | Tidak ada pembatasan model per paket membership. Konsekuensi alami: model mahal membakar token lebih cepat → habis lebih cepat → mendorong topup. Self-balancing tanpa aturan ekstra. |

---

## Keputusan LOCKED — UX/UI (diskusi 2026-09-12)

| ID | Keputusan | Detail |
|---|---|---|
| U-01 | **Onboarding preset via wawancara/form — bukan wizard paksa** | Setting preset bisnis dipilih SETELAH wawancara dengan AI atau human (sales/onboarding call), atau setelah user isi form onboarding. Tidak ada wizard paksaan saat login pertama. Detail mekanisme wawancara: OPEN (lihat Q-05). |
| U-02 | **Halaman pengaturan = satu halaman friendly dengan tab-tab** | Buka satu halaman, navigasi via tab. Struktur tab DINAMIS dari tab registry (tidak hardcode) — tab muncul sesuai role user & feature flag tenant. Super Admin lihat lebih banyak tab, owner tenant lihat tab operasional saja. |
| U-03 | **Dashboard per industri = card khusus kontekstual** | Masing-masing industri punya card/widget khusus: Apotek = Antrean Resep Hari Ini + Stok Expiry Dekat; Rental = Unit Tersewa + Booking Hari Ini + Jatuh Tempo Pengembalian; F&B = Meja Aktif + Open Bill; Kontraktor = Progres Proyek + Retensi Tertahan; EO = Event Mendatang; Agency = Pipeline Deal + Timesheet. |
| U-04 | **Sidebar zero-bloat: tidak dipakai = tidak muncul** | Modul/fitur yang tidak diaktifkan tenant SAMA SEKALI tidak dirender (bukan disabled/greyed-out, tanpa badge "Upgrade"). |
| U-05 | **Topup token satu tempat, subtle — bukan CTA jualan** | Topup hanya muncul di satu tempat (dalam konteks tab Penggunaan & Paket), didesain natural/integrated seperti informasi kuota, BUKAN banner/tombol CTA mencolok yang terlihat jualan token. |
| U-06 | **Dynamic Theme (Non-Hardcode)** — *sebagian digantikan D-43* | Dukungan ganti skema warna branding tenant (katalog tema aman WCAG AA) tanpa hardcode CSS, berbasis 36 token `--erp-*`. **KOREKSI D-43:** klausa "toggle mode terang/gelap **personal per user**" **DIBATALKAN** — tema (termasuk mode terang tema E) adalah pengaturan **per-USAHA** yang di-set owner dan berlaku untuk semua staf company itu. Tidak ada preferensi tema per akun/browser. |
| U-07 | **Universal Search (Ctrl+K) via Laravel Scout** | *Command Palette* berfungsi sebagai mesin pencari global (*Database/Meilisearch*). Mencari kontak, tagihan, dan navigasi lintas modul dalam satu ketikan, dengan *scoping* ketat per `company_id`. |
| U-08 | **App Switcher Terisolasi (Odoo-Style)** | *Sidebar* tidak dijadikan satu (*Unified*). Saat menekan ikon 9-titik, Klien memilih Modul (misal: HRD). Setelah diklik, layar dan *Sidebar* murni hanya menampilkan menu HRD. Sangat fokus, bersih, dan bebas *bloatware*. |
| D-16 | **Urutan Eksekusi Dimulai dari UI Kernel & Dynamic Sidebar (Opsi B)** | Bongkar sidebar bloated dan bersihkan antarmuka terlebih dahulu via `DynamicMenuRegistry` & middleware `EnsureFeatureEnabled`. UI memakai Laravel 13 + Livewire v4 + Blade + Tailwind v4 + Vite (Alpine ikut dari bundel Livewire, bukan paket terpisah); modul tersembunyi jika dimatikan. |
| D-17 | **Mode Pajak Campuran / Add-on Berbayar / Enterprise** | Konfigurasi dasar adalah satu mode pada `BusinessIdentity`: Non-PKP atau PKP dengan harga inklusif/eksklusif. Override inklusif/eksklusif per item/transaksi atau multi-tax rate hanya dijadikan add-on berbayar/paket Enterprise setelah kontraknya ditetapkan. |
| D-18 | ~~**Data Model Decoupled Modular - Warisan Paving Dipertahankan**~~ - **DIGANTIKAN D-31/D-33 (review G-12, 2026-09-16)** | Keputusan asli: tabel rantai pasok warisan pabrik (SPH, PO bertingkat, DO, Jurnal Manufaktur) dipertahankan di balik feature flag. **Tidak berlaku lagi** karena D-31 meruntuhkan tabel per-industri menjadi kapabilitas generik, dan tidak satu pun tabel warisan itu ada di `DATA_MODEL.md`. Penggantinya: (a) **SPH/penawaran** -> kapabilitas `quotations` (`DATA_MODEL.md` 4.5); (b) **jurnal** -> `finance.accounting` double-entry (4); (c) **PO bertingkat, DO, jurnal manufaktur (BOM multi-level + WIP)** -> **modul Tier B manufaktur yang BELUM dibangun**, menunggu keputusan Bos di **T-25** (D-33/Fase 6). Artinya: klien manufaktur bertingkat **belum dapat dilayani** sampai T-25 diputuskan dan T-25b dibangun - jangan dijanjikan di materi penjualan. Klien korporat non-manufaktur tetap terlayani penuh lewat kapabilitas Tier A + Enterprise (D-52/D-54). |
| D-19 | **Granular Config Per-Module Per-Company** | Setiap modul memiliki skema konfigurasi JSON independen per tenant (tabel `module_settings` dengan field `module_name`, `company_id`, `settings_json`). Contoh CRM: Company A bisa aktifkan pipeline + deal stage custom, Company B hanya aktifkan phonebook kontak + auto-sync WhatsApp tanpa form rumit. |
| D-20 | **UX Visual Redesign: Midnight Command + Operator Grid Hybrid** | Varian prototype terpilih: **A (Midnight Command)** untuk dashboard & shell eksekutif (dark-first, rail sidebar ikon, command palette `Ctrl K`, omzet hero, KPI sparkline, feed agentic); **C (Operator Grid)** untuk halaman operasional (telemetri kas/kuota/sync di topbar, tabel padat, log live, monospace angka). Semua path mengikuti skema `/app/{module}/...` (lihat D-24). |
| D-21 | **Autentikasi Tradisional (Web Login)** | Menggunakan Email dan Password standar untuk akses masuk ke aplikasi Web (SaaS). WhatsApp secara eksklusif hanya digunakan sebagai saluran komunikasi asisten AI (Hermes), bukan sebagai gerbang otorisasi Web. |
| D-22 | **Bring Your Own Storage (BYOS) via Google Drive** | Semua lampiran fisik (*file* foto, nota, kontrak) di-upload dan disimpan di Google Drive milik Klien melalui integrasi API. Sistem SaaS hanya menyimpan *URL link* ke dalam satu tabel *Polymorphic* bernama `attachments`. Menghemat biaya *server storage* secara drastis. |
| D-23 | **SaaS Billing via Proactive WA Invoice** | Penagihan biaya langganan aplikasi tidak memotong kartu kredit otomatis. Sebaliknya, Asisten AI (Hermes) secara proaktif membuat tagihan (*Invoice*) dan mengirimkan *Link* Pembayaran (QRIS/VA) ke WA Bos pada H-3 sebelum masa tenggang. Begitu dibayar, masa aktif *Membership* otomatis bertambah. |
| D-24 | **Skema Routing Tunggal `/app/{module}/{path?}`** (terverifikasi dari kode 2026-09-16) | Seluruh halaman aplikasi berada di bawah route bernama `app.module` dengan URL `/app/{module}/{path?}`. Lobby App Switcher di `/`. Contoh: `/app/settings`, `/app/accounting/reports`. Tidak ada `/dashboard`, `/settings`, atau `/operations/*` di level root. Registry menu menyimpan **URL path**, bukan nama route Laravel per fitur. Dokumen lama yang memakai skema lain (INDUSTRY_PRESETS §2 versi awal, UX_UI_SPEC) telah dikoreksi. |
| D-25 | **Bentuk `module_settings` mengikuti D-19** | Satu baris per `(company_id, module_name)` dengan kolom `settings_json`. Feature flag disimpan di modul `features` sebagai `settings_json = {"crm.leads": true, ...}`. Tidak ada baris key-value per flag. |
| D-26 | **Kolom `company_id` wajib di SEMUA tabel bisnis** | Termasuk tabel anak seperti `accounting_journal_lines`, `crm_activity_logs`, `eo_vendors`. Tidak boleh mengandalkan join ke parent untuk isolasi tenant. |
| D-27 | **Konfirmasi 2-langkah memakai kode tiket** | Format balasan Bos adalah `YA <kode-tiket>`, bukan `YA` polos, agar tidak dapat diputar ulang (replay). Kode tiket = 4-6 digit yang dikirim sistem bersama Kartu Persetujuan. Menyelaraskan REQUIREMENTS §3.4 dan COMMERCIAL §4 dengan D-11. |
| D-28 | **Model AI tidak dibatasi paket** | Menegaskan D-15. Semua paket dapat memilih model apa pun dari katalog. Perbedaan hanya pada kecepatan habisnya token via `ai_model_pricings`. COMMERCIAL §2.2 tidak boleh dibaca sebagai pembatasan per paket. |
| D-29 | **Foto/lampiran mengikuti BYOS (D-22) tanpa kecuali** | `pharmacy_prescriptions.image_path` dihapus; foto resep disimpan sebagai baris `attachments` polimorfik. |
| D-30 | **Product naming canonical** | Produk front-facing: **NalarPesan**. Produk back-office: **Agentic BOS**. Istilah "ERP Prime" dan "ERP Nalarin" adalah nama warisan dan tidak dipakai di kode/UI baru. |
| D-31 | **Composable Capability Architecture — "industri = data, kapabilitas = kode"** (2026-09-16) | Target produk adalah **puluhan jenis bisnis** (≥50), bukan 6. Karena itu: (a) feature flag menyatakan **kapabilitas generik** lintas industri (`projects`, `bookings`, `inventory.batch_expiry`), **bukan** nama industri (`ops.contractor_spk`); (b) sebuah **preset industri adalah data** — satu baris `business_presets.definition` berisi kapabilitas aktif, terminologi, workflow default, dan susunan widget dashboard — bukan kode; (c) **alur bisnis** didefinisikan sebagai data di `workflow_definitions` dan dieksekusi satu `WorkflowEngine`; (d) **terminologi UI** diambil dari `terminology_map` preset/company via helper `term()`, tidak di-hardcode di Blade; (e) entitas yang variatif memakai kolom `attributes JSON` + `type`, dan tabel domain khusus **hanya** dibuat bila ada aturan bisnis yang tidak bisa direpresentasikan sebagai data (FEFO, retensi, double-entry, regulasi). Menambah industri baru **tidak boleh** memerlukan migration atau komponen Blade baru. Detail: `INDUSTRY_PRESETS.md` §0–§3, `DATA_MODEL.md` §1.5–1.7. |
| D-32 | **Katalog kapabilitas v1 dikunci (21 kunci kapabilitas)** | `contacts`, `deals`, `projects`, `projects.progress_billing`, `scheduling`, `bookings`, `bookings.deposit`, `inventory`, `inventory.batch_expiry`, `inventory.bom`, `pos`, `pos.tables`, `quotations`, `milestone_billing`, `approval_flow`, `timesheet`, `finance.cashbook`, `finance.accounting`, `hr.employees`, `hr.payroll`, `system.ai_agent`. **Tier A (21 kunci di atas) bukan daftar lengkap seluruh kunci kapabilitas**: kunci **Tier B** (`pharmacy.prescription`, `construction.retention` — D-33) juga sah dan terdaftar di `INDUSTRY_PRESETS.md` §1. `PresetDefinitionValidator` (T-F4) wajib menerima **Tier A + Tier B** sebagai kunci valid; menolak Tier B akan membuat preset apotek/kontraktor gagal validasi. Menambah kapabilitas baru (tier mana pun) = keputusan arsitektur (butuh Bos), menambah preset baru = data (tidak butuh Bos). |
| D-33 | **Dua tier cakupan industri** | **Tier A (komposisi murni, target ≥80% jenis bisnis):** preset dibentuk hanya dari kapabilitas D-32 + terminologi + workflow + widget — nol kode. **Tier B (modul domain khusus):** industri dengan aturan yang tidak bisa jadi data (farmasi/obat keras, konstruksi/retensi-opname, manufaktur/BOM bertingkat) mendapat modul kode yang dibangun **sekali** dan dipakai industri serumpun. 6 preset awal: Agency, F&B, EO, Persewaan = Tier A; Apotek, Kontraktor = Tier A + 1 modul Tier B masing-masing. |
| D-34 | **Payment gateway: Midtrans** (2026-09-16, ex-Q-01) | Signature `SHA512(order_id + status_code + gross_amount + ServerKey)`; env `MIDTRANS_SERVER_KEY`, `MIDTRANS_IS_PRODUCTION`. Vocabulary status mengikuti notifikasi Midtrans. Abstraksi `PaymentGateway` interface agar Xendit bisa ditambah tanpa mengubah T-19. |
| D-35 | **Scout driver `database`** (ex-Q-02) | Nol infra untuk 500 tenant awal. Migrasi ke Meilisearch hanya bila p95 pencarian > 500 ms terukur. |
| D-36 | ~~**Palet tema diturunkan agent dari Tailwind v4**~~ (ex-Q-03) — **DIGANTIKAN D-43.** Nama tema lama (`Prime Default`, `Clean Ledger`, `Ocean Blue`, `Brass Amber`, `Rose`) **tidak dipakai**; yang berlaku 5 tema A/B/C/D/E dengan nilai hex lengkap di `UX_UI_SPEC.md` §7.3a. Sisa yang tetap berlaku dari D-36: kewajiban WCAG AA + test kontras. Teks historis: 5 tema dari slate/emerald/sky/amber/rose. Setiap pasangan teks/latar WCAG AA >= 4.5:1, dibuktikan test unit penghitung kontras. 36 token di UX_UI_SPEC 7. |
| D-37 | **Bot Hermes: SATU per OWNER, multi-bisnis; bot tambahan = add-on berbayar** (ex-Q-04, **menolak default**) | Owner dengan N company memakai **satu** bot WA yang sadar konteks: bot tahu semua company milik owner, menanyakan/menyimpulkan company yang dimaksud, dan menyimpan `active_company_id` per percakapan. Tujuan: owner cukup punya satu nomor asisten. **Bot tambahan** (mis. bot khusus manajer cabang dengan scope 1 company + role terbatas, atau bot pribadi kedua untuk Bos) adalah **add-on** dengan biaya tambahan per bot. Skema: `hermes_profiles.owner_user_id` (bukan `company_id UNIQUE`), tabel pivot `hermes_profile_companies` (scope company + role), `type ENUM('primary','addon')`, `billing_addon_id`. Kuota AI dihitung per owner (primary) dan per add-on. COMMERCIAL 3.2 menjadi sumber kebenaran; PRD/DATA_MODEL disesuaikan. |
| D-38 | **Stage CRM: kode netral, label lokal** (ex-Q-05) | `stage` VARCHAR berisi `new/qualified/proposal/negotiation/won/lost`, divalidasi `WorkflowEngine`; label Indonesia via `term()`/preset; custom per company via `workflow_definitions`. |
| D-39 | **Vendor string terlarang di UI tenant** (ex-Q-06) | `Hermes`, `Nous`, `Nous Research`, `Laravel`, `Midtrans`, dan `laravel/laravel` di `composer.json name`. Diaudit T-22. |
| D-40 | **Onboarding: form web dulu** (ex-Q-07) | Wawancara AI via WA menyusul setelah node API Hermes tersedia (T-17b). Human call opsional untuk Enterprise. |
| D-41 | **Konteks tenant aktif: `users.current_company_id`** (ex-Q-08; diperjelas 2026-09-16) | Target produksi Fase 3 adalah kolom + middleware; bukan subdomain/session. **Pengecualian adapter Fase 2 yang disetujui Bos:** `JsonCompanyContext` boleh menerima `?company=` hanya di environment `local`/`testing`, hanya untuk tiga company demo allowlist, lalu menyimpan pilihan tervalidasi di session. Di environment lain query switch dan session sebagai sumber tenant ditolak (fail-closed). Pengecualian ini hanya untuk demo sebelum auth/company persistence tersedia dan wajib diganti oleh `users.current_company_id` di Fase 3. Selaras D-21 dan D-37 (bot juga menyimpan `active_company_id` per percakapan, sinkron dengan kolom ini bila user sama). |
| D-42 | **Frontend-first: aplikasi utuh dari JSON dulu, database menyusul** (2026-09-16) | Seluruh layar Fase 2 dibangun dengan sumber data JSON di balik interface (`EntityRepository`, `PresetSource`, `CompanyContext`); Fase 3 mengganti `Json*` -> `Eloquent*` via `DATA_SOURCE` tanpa mengubah Blade. **JSON menggantikan tabel, bukan logika**: preset di `database/presets/`, skema entitas di `database/schemas/` (sumber migration), baris per company di `storage/app/json/{company}/`. Enam pola layar generik, bukan satu Blade per kapabilitas. Anti-hardcode ditegakkan test arsitektur permanen (T-F14) dan bukti `laundry.json` (T-F15). Gate `HUMAN:UI-LOCK` bergeser: dikunci setelah melihat aplikasi utuh 4 preset. |
| D-43 | **Palet default: A (Slate + Emerald); 5 tema tetap, di-set per USAHA oleh owner** (Bos, 2026-09-16) | Nilai di `UX_UI_SPEC.md` §7.3a kolom A menjadi default §7.3/`app.css`. 5 tema tetap: A (default), B (Zinc+Amber), C (Navy+Sky), D (Stone+Terracotta), E (mode terang dari A) via `[data-theme]`. **Cakupan: per-company, bukan per-user.** Owner memilih tema di `/app/settings` tab Tampilan; tersimpan di `companies.theme` (server-side); **seluruh staf company itu melihat tema yang sama** — konsisten untuk branding usaha ke pelanggan/pegawai. Tidak ada toggle gelap/terang personal per akun/browser (`localStorage`) — kartu "Preferensi Mode Gelap/Terang Personal" di mockup **tidak sesuai keputusan ini** dan harus diperbaiki jadi murni pemilihan tema per-usaha. Accent picker custom (opsional, jika diaktifkan) tetap per tenant, tervalidasi kontras server-side sebelum disimpan. Success pada tema A dipetakan emerald-300 agar berbeda dari aksen. |
| D-44 | **Non-PKP tidak pernah melihat kosakata pajak** (2026-09-16, dari temuan UX mockup) | Bila `business_identities.tax_mode=non_taxable` (D-03), baris `DPP`, `PPN`, dan istilah pajak **tidak dirender sama sekali** (zero-bloat U-03) - bukan ditampilkan bernilai 0. Struk/faktur hanya menampilkan Subtotal dan Total. Onboarding (T-F12) menanyakan sekali dengan bahasa awam: *"Usaha Anda memungut PPN (sudah PKP)?"* default **tidak**. Perhitungan `TaxRateService` tidak berubah; yang diatur hanya penyajian. Mayoritas dari 63 bisnis target adalah non-PKP. |
| D-45 | **Konfirmasi berisiko bertingkat menurut akibat** (diperbarui dari temuan UX lapangan) | **Tingkat 1 - ketik `YA`:** hanya untuk aksi **tidak dapat dibatalkan dan berdampak legal/fiskal** - void transaksi yang sudah dibayar, hapus order berbayar, hapus data massal, reset. **Tingkat 2 - dua tombol (Batal / aksi merah):** destruktif tapi dapat dipulihkan atau belum berdampak uang - hapus draf, batalkan booking belum bayar, mundurkan tahap. **Tingkat 3 - tanpa konfirmasi:** aksi reversibel biasa. **Tekan-dan-tahan dilarang di semua tingkat.** Anti salah-tap: tombol aksi di modal **inert 400 ms** sejak modal muncul (mencegah tap pembuka ikut menekan), bukan penundaan setelah klik. Input ternormalisasi `strtoupper(trim())`, atribut keyboard `autocapitalize=characters autocorrect=off`. Teks modal wajib menyebut akibat konkret + nominal. `YA <kode-tiket>` (D-27) tetap khusus WhatsApp. **Tingkat ditentukan per aksi di kode / per transisi di preset (`confirm: type\|simple\|none`), bukan per layar.** |
| D-46 | **Integritas alur kerja: tidak boleh ada jalan buntu** (dari temuan UX lapangan) | `PresetDefinitionValidator` **menolak** definisi workflow bila: (a) ada stage tidak terjangkau dari stage awal; (b) ada stage non-terminal tanpa transisi keluar (dead end); (c) `terminal` tidak dideklarasikan eksplisit. **Konvensi wajib:** setiap tahap pemeriksaan/QC harus punya **cabang mundur (rework loop)** - mis. bengkel `qc -> [pengerjaan, siap_diambil]`. Transisi **mundur** (ke stage yang lebih awal) wajib `requires_note: true` sehingga alasan tercatat di log - ini yang membedakan koreksi sah dari manipulasi. Staf tidak boleh pernah butuh admin database untuk memperbaiki kesalahan operasional. |
| D-47 | **U-01 tetap berlaku (owner self-onboarding); Super Admin boleh membantu setup atas nama klien, dengan jejak audit** (Bos, 2026-09-16) | Dua jalur tidak saling meniadakan: (a) owner bisa onboarding sendiri (U-01, default); (b) **Super Admin platform bisa login sebagai klien** untuk klien yang tidak sempat/kurang paham teknologi, memakai **"Login As" beraudit** - bukan tahu password klien, bukan akun bersama. Implementasi: `admin_impersonation_sessions` (admin_user_id, company_id, started_at, ended_at, reason, ip). Selama sesi impersonasi: banner kuning permanen "Anda login sebagai {company} - mode Bantuan Admin" tidak bisa ditutup; **aksi finansial/destruktif tetap butuh D-45 tingkat 1/2 seperti biasa** (impersonasi tidak melewati approval); semua perubahan `module_settings`/`business_preset`/`companies.theme` selama sesi dicatat dengan `changed_by_type=admin_impersonation` di log yang sudah ada (D-46 `requires_note` style), bukan tercatat seolah dilakukan owner. Super Admin **tidak bisa** melihat/expor data sensitif klien (password, secret) selama impersonasi - hanya operasi setup (preset, tema, istilah, kapabilitas, undang staf). Butuh T-00c (auth) selesai lebih dulu; jadi task baru **T-17c "Login As" Super Admin** ditambahkan setelah T-00c di Fase 3a/4, `BLOCKED` sampai panel Super Admin (belum ada task-nya - dicatat sebagai kebutuhan Fase 4/5, bukan Fase 2). |
| D-48 | **Saldo token habis: turun otomatis ke model murah, bukan mati total** (Bos, 2026-09-16) | Saat `current_token_balance <= 0`, sistem **tidak** mematikan asisten. Bot otomatis turun ke **model fallback termurah** (katalog `ai_model_pricings` dengan multiplier terendah, mis. kelas haiku/qwen) dengan **kuota darurat terbatas** (`emergency_token_quota` di `membership_plans`, default disarankan 5% kuota bulanan) sampai owner topup. Selama mode darurat: (a) bot memberi tahu sekali di awal percakapan *"Saldo AI menipis, saya beralih ke mode hemat"* - tidak diulang tiap pesan (U-05: tidak boleh terasa jualan); (b) kualitas jawaban turun tapi operasional tidak berhenti; (c) `token_ledger_entries` tetap mencatat debit mode darurat agar terekonsiliasi. Bila kuota darurat **juga** habis: bot berhenti membalas dengan pesan jelas + link topup, **web tetap berfungsi penuh**. Alasan: kasir/klinik yang botnya mati mendadak di jam sibuk adalah kegagalan produk, bukan sekadar kegagalan bayar. |
| D-49 | **Langganan telat bayar: penurunan bertahap, bukan pemutusan mendadak** (Bos, 2026-09-16) | Tahapan setelah jatuh tempo (`company_memberships.expires_at`): **H-3** invoice proaktif via WA (D-23). **H+0** asisten AI berhenti (bot mati), **web tetap penuh**. **H+7** web menjadi **hanya-baca**: semua data terlihat dan **dapat diekspor** (D-51), input/edit/transaksi baru ditolak dengan pesan jelas + link bayar. **H+30** akun **dibekukan** (login ditolak, data tetap utuh di server). **H+90** data boleh dihapus permanen, **hanya setelah 3 peringatan tercatat** (H+30, H+60, H+83) ke WA + email owner. Pembayaran di tahap mana pun memulihkan akses **seketika** tanpa kehilangan data. Tidak ada tahap yang boleh menghapus data tanpa jejak notifikasi yang bisa dibuktikan. |
| D-50 | **Kepatuhan UU PDP disiapkan sejak awal, bukan menyusul** (Bos, 2026-09-16) | Produk menyimpan data pribadi pelanggan klien (nomor WA, nama) dan - untuk preset klinik/apotek - **data kesehatan** yang tergolong data pribadi spesifik menurut UU 27/2022 (PDP). Kewajiban minimum yang masuk lingkup produk: (a) **Kebijakan privasi & persetujuan** - teks persetujuan saat onboarding, dicatat waktunya; (b) **enkripsi at-rest** untuk kolom sensitif (identitas pasien, catatan medis, NIK bila ada) - bukan sekadar TLS; (c) **log akses** siapa membuka data pasien (`access_logs`, wajib untuk kapabilitas bertanda sensitif); (d) **hak subjek data**: ekspor & hapus data pelanggan atas permintaan, dijalankan owner tenant dari UI; (e) **retensi**: data dihapus sesuai D-49 H+90, bukan disimpan selamanya; (f) **pemrosesan oleh AI**: data pasien **tidak** dikirim ke model AI kecuali owner mengaktifkan secara eksplisit per-kapabilitas (default **mati** untuk kapabilitas bertanda sensitif). Platform bertindak sebagai **prosesor**, tenant sebagai **pengendali** data - dinyatakan di perjanjian layanan. Task teknis diturunkan di Fase 4/5 (belum dibuat; dicatat sebagai kebutuhan wajib sebelum preset klinik/apotek dijual). |
| D-51 | **Trial 14 hari penuh fitur + ekspor data selalu tersedia** (Bos, 2026-09-16) | **Trial:** 14 hari, **fitur penuh**, tanpa kartu kredit, dengan **kuota token kecil** (`trial_token_quota`, disarankan 10% Starter). Berakhir tanpa bayar -> masuk alur D-49 mulai tahap H+7 (hanya-baca + bisa ekspor), bukan langsung hilang. Satu owner = satu trial (dicegah lewat `wa_number` + email, bukan hanya email). **Ekspor data (berlaku selamanya, bukan hanya trial):** owner dapat mengunduh seluruh data usahanya kapan saja dari `/app/settings` dalam format terbuka (CSV per entitas + JSON preset/pengaturan), tanpa perlu meminta ke admin platform, **termasuk saat status hanya-baca/menunggak**. Alasan: "data saya tidak disandera" adalah penghalang jualan nyata dan sekaligus kewajiban D-50(d). Lampiran tetap di Google Drive milik klien (D-22) sehingga otomatis sudah di tangan mereka. |
| D-52 | **Paket membatasi KAPABILITAS, bukan nama preset** (Bos, 2026-09-16) | Bos memilih "preset kompleks khusus paket atas". Agar tidak melanggar D-31 (sistem tidak boleh mengenal nama industri), pembatasan diterapkan pada **kunci kapabilitas** (D-32), bukan pada `business_preset`. `membership_plans.features` berisi daftar kapabilitas yang terbuka untuk paket itu. Pembagian awal (dapat diubah Super Admin, D-05): **Starter** - `contacts, deals, scheduling, bookings, inventory, pos, quotations, finance.cashbook, hr.employees, approval_flow, system.ai_agent`; **Pro/Growth** - semua Starter + `projects, milestone_billing, timesheet, bookings.deposit, inventory.batch_expiry, pos.tables, hr.payroll`; **Enterprise** - semua + `projects.progress_billing, finance.accounting, inventory.bom` + seluruh modul Tier B (`pharmacy.prescription`, `construction.retention`). Konsekuensi alami: preset kontraktor/apotek/manufaktur **tidak dapat aktif penuh** di Starter karena kapabilitas intinya terkunci - tanpa sistem pernah menyebut nama industri. Kolom `membership_plans.allowed_presets` **dihapus** dari skema (COMMERCIAL §2.1 dikoreksi); saat onboarding, preset yang menuntut kapabilitas di luar paket ditampilkan dengan label *"perlu paket lebih tinggi"* beserta daftar kapabilitas yang kurang - bukan disembunyikan (pengecualian sah dari U-04, karena ini informasi harga bukan bloat fitur). |
| D-53 | **Dua kuota berbeda memang disengaja: GRUP dari paket, BOT dari add-on** (Bos, 2026-09-16) | Menyelesaikan tumpang tindih D-10 vs D-37. **Kuota grup WA** (`membership_plans.max_wa_groups`) membatasi berapa grup (Kasir/Gudang/Keuangan) yang boleh dimasuki bot `primary` milik owner - ini bagian dari paket. **Jumlah bot** tetap: 1 bot `primary` per owner (D-37, lintas semua company miliknya), bot berikutnya (`type=addon`) adalah **item add-on berbayar per bot**, masing-masing punya kuota grup dan kuota token sendiri. Tabel simulasi COMMERCIAL §2.3 baris "Karyawan AI (Hermes): 1/3/Unlimited Asisten" **dikoreksi**: angka itu adalah **kuota grup**, bukan jumlah bot. Enterprise "unlimited" berarti `max_wa_groups` besar/nol-batas, tetap 1 bot primary + add-on sesuai kebutuhan. |
| D-54 | **Enterprise = VPS dedicated yang DIKELOLA platform; on-premise sejati dihapus** (Bos, 2026-09-16) | COMMERCIAL §2.3 "Dedicated VPS / **On-Premise**" dikoreksi menjadi **"Dedicated VPS (dikelola platform)"** saja. Alasan: server di tempat klien memutus metering token (D-14 key terpusat), menyulitkan pembaruan, dan membocorkan jejak white-label (D-09). Enterprise tetap dapat: database & node Hermes dedicated via `TenantProvisioner` (D-06), isolasi jaringan, dan SLA khusus - tetapi infrastruktur tetap di bawah kendali platform. Permintaan on-premise sejati ditangani sebagai **kontrak khusus di luar produk standar** (butuh lisensi tahunan + mekanisme metering offline), bukan opsi katalog; jangan dijanjikan di materi penjualan sebelum kontraknya ada. |
| D-55 | **Nomor WhatsApp asisten: default milik KLIEN sendiri; disediakan platform = add-on berbayar** (Bos, 2026-09-16) | **Default (termasuk paket termurah):** klien memakai **nomor WhatsApp miliknya sendiri** sebagai asisten - owner scan QR dari Web BOS, nomornya tersambung ke engine (mekanisme QR Bridge, COMMERCIAL §4 Lapis 6). Tidak ada biaya tambahan, nomor tetap milik dan kendali klien. **Add-on berbayar:** platform **menyediakan** nomor WhatsApp untuk klien yang tidak ingin memakai nomor pribadi, tidak mau beli SIM/perangkat, atau ingin nomor bisnis yang tidak ikut hilang saat staf keluar. Biaya add-on menutup pengadaan nomor, perangkat/sesi, dan risiko pemeliharaannya. Konsekuensi arsitektur: jalur default **tidak** boleh mengasumsikan platform punya stok nomor; penyediaan nomor adalah kapabilitas tambahan, bukan prasyarat onboarding. |
| D-56 | **Katalog add-on resmi (dipilih Bos 2026-09-16)** | Add-on dijual terpisah dari paket. **Sudah ada:** mode pajak campuran/multi-rate (D-17), bot tambahan per unit (D-37), topup token (D-08), kuota grup WA ekstra (D-53). **Disetujui untuk direncanakan:** (a) **Cabang/lokasi tambahan** - pengungkit harga utama, arsitektur multi-company sudah siap (D-06/D-41); (b) **e-Faktur/Coretax** untuk klien PKP - wajib regulasi, sangat lengket; (c) **Loyalty pelanggan** (poin/voucher) - satu bangun dipakai lintas ritel/F&B/salon/laundry; (d) **Integrasi marketplace/ojol** (GoFood/GrabFood/marketplace) - order masuk otomatis, nilai besar untuk F&B & toko; (e) **Payroll lanjutan** (BPJS/PPh21) - perluasan `hr.payroll`; (f) **Nomor WhatsApp disediakan platform** (D-55) dan **domain/struk ber-merek sendiri** - white-label lebih dalam dari D-09; (g) **Penyimpanan terkelola** - alternatif BYOS (D-22) bagi klien tanpa Google Drive. **Tidak dipilih saat ini:** jasa setup berbantuan sebagai item berbayar (kemampuannya tetap ada via D-47, hanya belum dijual terpisah), akses API, dukungan prioritas/SLA. **Aturan wajib:** setiap add-on diaktifkan lewat mekanisme kapabilitas (D-52) atau item billing, **tidak boleh** menambah nama industri ke kode (D-31); add-on yang butuh aturan domain khusus tunduk pada Tier B (D-33). Urutan pembangunan ditentukan permintaan pasar nyata, bukan ditebak sekarang. |

---

| D-57 | **Modul Tier B Tambahan: manufacturing.production_order** | Dipilih langsung oleh Bos (2026-09-18) untuk task T-25. Memungkinkan industri manufaktur yang membutuhkan BOM multi-level dan pencatatan WIP. |

| D-58 | **Add-on pertama Fase 6b: Cabang/Lokasi Tambahan** | Dipilih langsung oleh Bos (2026-09-18) sebagai add-on pertama dari katalog D-56 yang dibangun. Desain: `companies.parent_company_id` self-referencing FK (cabang = child company yang berbagi `owner_user_id`); billing per cabang via `company_memberships` terpisah per row (tidak menggandakan skema); laporan gabungan lintas cabang dibatasi ke company dalam grup kepemilikan yang sama (root + children), diakses hanya oleh owner. Diaktifkan lewat kapabilitas (D-52), bukan flag industri (D-31). |
| D-59 | **Loyalty pelanggan (poin/voucher) disahkan sebagai kapabilitas generik opsional** (Bos, 2026-09-19, untuk T-35) | Kapabilitas `addon.loyalty`, opt-in per company lewat `ModuleSetting`/`PlanCapabilityGate` (D-52), bukan default aktif. **Aturan hitungan poin TIDAK BOLEH hardcode satu rumus** — setiap company memilih mode + rasio sendiri saat setup: (a) **per-nominal transaksi** (mis. tiap Rp X = 1 poin, rasio dikonfigurasi per company), (b) **per-item/kategori** (mis. produk/kategori tertentu bernilai poin berbeda, dikonfigurasi per company), (c) **keduanya sekaligus** — company bisa gabung kedua mode. Aturan ini disimpan sebagai data konfigurasi (`loyalty_rules`/serupa), bukan `if` di kode. **Expiry poin opsional per company**: company memilih sendiri apakah poin kedaluwarsa (dengan durasi dikonfigurasi, mis. 6/12 bulan) atau tidak pernah kedaluwarsa — default aman: tidak expire kalau company tidak mengatur apa pun. Tetap tunduk D-26 (`company_id` di semua tabel baru) dan D-31 (tidak ada nama industri di kode). |
| D-60 | **Company tanpa paket berbayar memakai TIER GRATIS KHUSUS (config-driven, bukan hardcode)** (Bos, 2026-09-20) | Ketika company tidak punya `company_memberships` aktif (atau plan-nya null), berlaku **tier gratis** yang disimpan sebagai **data konfigurasi** (bukan `if`/hardcode di kode). **Strategi freemium Bos — buat ketagihan:** tier gratis MEMBUKA semua kapabilitas operasional + `system.ai_agent` (asisten AI) supaya klien merasakan kecanggihan penuh. **Pembatasan lewat KUOTA, bukan kapabilitas:** (a) `max_wa_groups = 1` (hanya 1 grup WA); (b) **kuota token AI kecil** — cukup untuk 1-2 hari coba-coba (angka persis dapat diubah Bos lewat konfigurasi, default awal misal 500 token). Implementasi: `PlanCapabilityGate` mengembalikan definisi tier gratis (semua kapabilitas + kuota) dari config saat tidak ada membership aktif, sehingga `FeatureResolver` tetap konsisten dan kuota tetap ditegakkan `TokenLedgerService`/guarding grup. Demo/test yang bergantung pada company tanpa paket memakai tier gratis secara eksplisit. Lihat task T-36. |
| D-61 | **Model trial = FREEMIUM BERBATAS KUOTA (Model C); setelah kuota habis WAJIB berbayar** (Bos, 2026-09-20) | Tier gratis (D-60) adalah freemium berbatas kuota, **bukan** berbatas waktu dan **tanpa** topup-pay-as-you-go. Semua fitur + AI terbuka selama kuota gratis (token + 1 grup WA) masih ada; **setelah kuota habis, klien HARUS memilih paket berbayar** — tidak ada opsi topup token kecil tanpa langganan pada jalur ini. Fail-closed: saat kuota habis, aksi AI/penambahan grup ditolak dan klien diarahkan ke halaman paket. Angka kuota dan isi paket tetap config-driven (D-60), sehingga Bos bisa mengubahnya tanpa menyentuh kode. |

| D-62 | **Tagihan pelanggan tenant adalah entitas tersendiri; laba-rugi proyek BASIS KAS** (Bos, 2026-09-22) | Entity `invoices` adalah tagihan langganan platform (D-23: `topup`/`subscription`, `company_membership_id`, `payment_url`) dan **tidak boleh** dipakai sebagai piutang tenant. Tagihan yang tenant terbitkan ke pelanggannya memakai entitas baru `customer_invoices` + `customer_invoice_lines` (**berbaris**, pilihan Bos), meniru kontrak `quotations` untuk penomoran unik per company dan triplet `subtotal`/`dpp`/`tax`/`grand_total` lewat `TaxRateService` — bukan rumus pajak baru. Tagihan boleh **berdiri sendiri atau menempel ke proyek** (`project_id` nullable). **Termin bertahap** memakai `project_milestones` yang sudah ada; `project_milestones.invoice_id` **di-repoint** dari `invoices` ke `customer_invoices` (kolom lama menyesatkan, tidak disisakan). **Pengeluaran per proyek** memakai `cash_entries.project_id` yang sudah ada — tidak ada tabel biaya baru. **Laba-rugi proyek dihitung BASIS KAS dan hanya membaca `cash_entries`**: pendapatan = arah `in` dengan `project_id`, biaya = arah `out` dengan `project_id`. Tagihan yang sudah terbit tapi belum dibayar **tidak** menaikkan pendapatan; piutang berjalan (terbit − terbayar) ditampilkan sebagai angka terpisah yang jelas bukan bagian laba. Layar wajib menyebut "basis kas" secara eksplisit supaya angkanya tidak dibaca sebagai akrual saat menetapkan harga. Laba-rugi akrual, jurnal akuntansi untuk tagihan, dan pembebanan biaya bahan/jam kerja ke proyek **di luar lingkup keputusan ini**. **Tidak ada kapabilitas baru** (D-32 tidak terpakai): gate-nya `milestone_billing` yang sudah ada. Dibangun di Fase 8 (T-41..T-47). |

| D-63 | **Pengingat piutang tenant dikirim lewat WhatsApp via Hermes** (Bos, 2026-09-22, untuk T-49) | Kanal pengingat piutang pelanggan = WhatsApp lewat Hermes node, konsisten dengan posisi produk sebagai asisten WA (D-60). Implementasi WAJIB memakai jalur company-scoped yang fail-closed (`App\Services\HermesNodeClient::sendWhatsAppMessage($companyId, $to, $message)`, pola yang sudah dipakai efek `NotifyOwnerWa`), **bukan** `sendWhatsApp()` pada `App\Contracts\HermesNodeClient` yang tidak ter-scope company. Pengingat harus **idempoten**: satu pengingat per tagihan per tahap, dicatat supaya replay scheduler tidak mengirim dua kali. Jadwal dan ambang hari bersifat **config-driven** (pola D-60), default awal: H-1 sebelum jatuh tempo lalu H+1, H+7, H+30 setelah jatuh tempo; Bos dapat mengubahnya tanpa menyentuh kode. Pengingat piutang tenant TIDAK boleh memakai `DunningLadder`/`BillingCheckExpiring` yang melayani tagihan langganan platform (D-23) — pihak yang ditagih berbeda. Aktivasi produksi tetap butuh `HUMAN:SECRET` (node/nomor Hermes produksi, lihat UR-05). |
| D-64 | **Akuntansi penuh (`finance.accounting`) dan payroll (`hr.payroll`) DIJUAL** (Bos, 2026-09-22, untuk T-54) | Kedua kapabilitas resmi menjadi bagian yang dijual, bukan sisa katalog yang menganggur. Konsekuensi yang wajib dipenuhi sebelum dinyalakan di preset mana pun: (a) `chart_of_accounts.schema.json` dan `payrolls.schema.json` dibuat — saat ini menu "Bagan Akun" menunjuk entity `cash_entries` dan "Payroll" menunjuk `employees`, keduanya salah sasaran; (b) pola layar `report` mendapat komponennya (`ReportScreen`) supaya Laporan Keuangan tidak jatuh ke kartu kontrak; (c) item menu `accounting/coa`, `accounting/journals`, dan `hrd/payroll` di-repoint ke entitas yang benar. `hr.payroll` sudah ada di paket Pro dan Enterprise, `finance.accounting` di Enterprise (`BosSeedPlans`), jadi penyalaannya di preset mengikuti gerbang paket (D-52) — bukan otomatis untuk semua tenant. |

| D-65 | **Keanggotaan tim: dua peran, undangan lewat WA, kuota `max_users` per paket** (Bos, 2026-09-22, untuk T-51) | Staf = **manusia dengan akun login sendiri**, bukan bot dan bukan baris `employees`. Menambah staf tidak menambah `hermes_profiles` dan tidak memakan `max_wa_groups`. (a) **Dua peran saja**: pivot `company_user` dengan `role` berisi `owner|staff`, dan `CompanyRoleResolver` membaca dari pivot itu, bukan hanya `companies.owner_user_id`. Kosakata peran TIDAK diperluas karena `PresetDefinitionValidator` mengunci `['owner','staff','system']` dan seluruh 40 preset memakainya di transisi workflow — menambah peran = perubahan sekelas D-32. Izin yang lebih halus, bila pilot menuntut, ditambahkan sebagai kolom izin per anggota (mis. `can_record_payment`), **bukan** sebagai peran baru. (b) **Undangan lewat WhatsApp via Hermes** (konsisten D-63): owner memasukkan nomor WA staf, sistem membuat undangan tertunda berkode sekali pakai berlaku 72 jam, terikat pasangan company + nomor, dapat dicabut owner, kedaluwarsa otomatis. Memakai jalur company-scoped fail-closed; aktivasi produksi butuh `HUMAN:SECRET`. (c) **Kuota**: tambah `max_users` ke `membership_plans` mengikuti bentuk `max_wa_groups` (+ `UserQuotaGate` meniru `WaGroupQuotaGate`, fallback config untuk tier gratis, fail-closed). Tier gratis = **1 pengguna** (owner saja) sesuai strategi kuota D-60/D-61; usulan awal Starter 3, Pro 10, Enterprise 100, dapat diubah Bos dari `/admin/plans`. Penegakan di titik undangan, bukan saat login. |

| D-66 | **Japri staf ke bot internal DIIZINKAN, baca-saja, lewat pintu otorisasi yang sama dengan web** (Bos, 2026-09-22, untuk T-58) | Prinsip utama: **bot tidak boleh melakukan apa pun yang web larang untuk orang itu.** Tidak ada tabel izin terpisah untuk WA — setiap aksi bot melewati `CompanyRoleResolver` dan aturan owner-only yang sama (T-50), sehingga WA tidak menjadi jalan memutar otorisasi web. (a) Japri staf **baca-saja**; aksi fiskal (menerbitkan tagihan, mencatat pembayaran), perubahan setting usaha, dan aksi destruktif tetap **owner-only** bahkan lewat WA. (b) **Nomor cocok saja tidak cukup**: wajib `users.wa_is_verified` benar **dan** keanggotaan `company_user` (D-65) aktif. Pencabutan keanggotaan harus langsung mematikan akses japri, dengan negative test. (c) Kapabilitas sensitif (`FeatureResolver::SENSITIVE_CAPABILITIES`: `pharmacy.prescription`, `addon.payroll_advanced`) **tidak pernah** dijawab pada japri staf; tunduk `AiDataSharingPolicy` (T-27e). (d) Bila satu nomor WA terdaftar sebagai anggota di lebih dari satu company, perilakunya **fail-closed saat ambigu** — bot menolak dan tidak menebak company — sampai T-37 (Multi-Tenant Context Switcher) mendarat. (e) Aturan grup WA-04 tidak dilonggarkan: di grup bot tetap hanya merespons saat di-tag sesuai `group_tag_only`. |

| D-67 | **NalarPesan DIKESAMPINGKAN; Agentic BOS tidak boleh menyentuhnya** (Bos, 2026-09-22) | NalarPesan (kanal WhatsApp, produk front-facing D-30) **dikeluarkan dari lingkup** sampai Bos mencabut keputusan ini. Aturan konkret: (a) **Jangan** membuka, menjalankan, menyunting, atau membaca konfigurasi repo/layanan NalarPesan dari dalam pekerjaan Agentic BOS; (b) **Jangan** menambah dependensi, webhook, cron, atau jalur deploy yang mengarah ke NalarPesan; (c) Pengiriman WhatsApp tenant tetap lewat `App\Services\HermesNodeClient` yang sudah fail-closed — tanpa node terdaftar ia **menolak**, dan itu perilaku yang benar, bukan bug untuk "diperbaiki" dengan menembak layanan lain; (d) ~~Hermes yang berjalan di PC pengembangan adalah **Hermes agent** (WebUI `9119` + gateway chat), **bukan** gateway WhatsApp — sudah diperiksa pada daftar rutenya, tidak ada endpoint kirim WA.~~ **KLAUSA INI SALAH DAN DICABUT (2026-09-22).** Yang benar: **hermes-webui** tidak punya endpoint WA, tetapi **Hermes agent punya** — `gateway_state.json` memuat platform `whatsapp` (Baileys) dan `whatsapp_cloud`, dan sesi Baileys ada di `platforms/whatsapp/session/`. Penyebab kekeliruan: `grep_search` tidak menjangkau luar folder workspace dan mengembalikan "no matches" tanpa peringatan. Lihat **D-68/D-70** dan **T-59** untuk koreksi lengkap di `AGENTS.md`, `HERMES.md`, `CLAUDE.md`. Yang **tetap berlaku** dari D-67: NalarPesan dikesampingkan, dan penolakan kirim oleh `HermesNodeClient` tanpa node terdaftar adalah perilaku benar; (e) Kontrak endpoint node WA yang dipakai kode saat ini adalah **asumsi** yang belum dikonfirmasi (lihat `docs/HERMES_NODE_CONTRACT.md`). Menyelaraskannya butuh keputusan Bos, bukan tebakan agent. |

| D-68 | **Hermes adalah mesin, Agentic BOS adalah produknya: white-label wajib di permukaan percakapan** (Bos, 2026-09-22) | Hermes tidak pernah dipublikasikan. Di permukaan mana pun yang dilihat pelanggan, yang menjawab adalah **Agentic BOS** — termasuk ketika ditanya "kamu siapa". Ini **memperluas D-39** dari UI (`resources/views`, `public/`, `composer.json`, dijaga T-22) ke permukaan percakapan, yang justru paling mungkin bocor karena modelnya memang tahu dirinya Hermes. Mekanismenya `SOUL.md` per profil (plus `default_soul.py`, `profile_describer.py`, `display.personality`); preseden white-label sudah ada di ekosistem (`BEJO_IDENTITY.md` di hermes-webui). **SOUL saja tidak cukup** — lima jalur bocor di luar kendalinya dan wajib ditangani sendiri: (a) jalur berkas (`C:\Users\...\AppData\Local\hermes\...`) yang muncul otomatis bila bot punya tool terminal; (b) nama tool ber-prefiks `hermes-`; (c) pesan non-model (banner, `/help`, prompt persetujuan, notifikasi update, string versi, galat CLI); (d) teks pesan kode pairing; (e) merek model (`model_catalog.url` menunjuk `hermes-agent.nousresearch.com`). **Dikecualikan: profil dev internal** — diputuskan Bos, karena memaksa bot dev berpura-pura menjadi Agentic BOS hanya menyembunyikan informasi yang dibutuhkan saat diagnosis. Verifikasi lewat skrip probe, bukan keyakinan. Lihat T-59, T-62, T-76. |
| D-69 | **Skill hidup di Hermes, aturan bisnis tetap di Agentic BOS** (Bos, 2026-09-22) | Skill bisnis ("susun proposal sesuai standar", "riset bisnis sesuai standar") adalah artefak prompt, jadi rumahnya Hermes. Batasnya keras: (a) skill **dilarang memuat aturan bisnis** — pajak, penomoran, dan transisi state tetap milik `TaxRateService`/`WorkflowEngine`; skill memanggil, tidak menghitung ulang, kalau tidak ada dua sumber kebenaran untuk uang; (b) skill **dilarang mengakses data** selain lewat TenantBot API; (c) profil yang dilihat pelanggan **dilarang** punya tool `terminal|shell|write_file|delete_file|git|process` (sudah tertulis sebagai `disallowed_tools` di `config/hermes.php`) — tanpa ini sebuah skill bisa menulis langsung ke basis data dan melewati `AuthenticateTenantBot` + `EnforceBotToolScoping`, sehingga seluruh otorisasi kita menjadi tidak relevan; (d) **satu skill melayani banyak tenant** — perbedaan per tenant datang sebagai **data** dari API kita, bukan berkas skill yang dipecah per industri; memecahnya adalah "industri = kode" yang dilarang D-31, hanya berpindah tempat ke Hermes. Keluaran mendarat di entitas yang sudah ada: proposal → `quotations` + `quotation_lines`, riset → `assistant_report`. Skill produk disimpan di repo (`hermes/skills/`) dan dipasang lewat `skills.external_dirs`, **bukan** di direktori `skills/` milik profil, karena `curator` (aktif, `stale_after_days: 30`, `archive_after_days: 90`, `prune_builtins: true`) dapat mengarsipkan atau memangkasnya. Kunci kapabilitas baru (D-32, penamaan didelegasikan Bos): `ai.proposal`, `ai.research`, `addon.cs_bot`. Lihat T-63, T-64, T-65. |
| D-70 | **Transport WhatsApp adalah data per company; jalur resmi hanya untuk CS** (Bos, 2026-09-22) | Dua transport: **`bawaan`** (Hermes/Baileys, pairing QR) dan **`resmi`** (Meta WhatsApp Cloud API, saat ini lewat **kirimdev**). Pilihan disimpan sebagai data per company, bukan hardcode, mengikuti pola D-34 (`PaymentGateway`) supaya penyedia bisa ditukar tanpa menyentuh pemanggil. **Jalur `resmi` hanya tersedia untuk bot CS**; bot operasional internal tetap `bawaan` karena penggunanya internal dan risikonya sudah terkelola — diputuskan Bos. Implementasi **wajib ditulis terhadap bentuk Meta Cloud API**, bukan bentuk kirimdev, karena endpoint kirimdev `/v1` kompatibel dengan format Meta; dengan begitu kirimdev hanyalah base URL + token dan perpindahan ke Meta langsung atau BSP lain menjadi perubahan konfigurasi. Alasan memilih resmi untuk CS: nomor CS akan disebar dan menerima volume dari orang asing — skenario paling rawan pemblokiran untuk Baileys; jalur resmi menghilangkan risiko itu plus memberi webhook bertanda tangan HMAC, retry, dan log yang bisa di-replay. Nama penyedia tidak pernah muncul di permukaan yang dilihat pelanggan (D-68), dengan **satu batas jujur yang tidak boleh dijanjikan sebaliknya**: layar persetujuan Meta saat onboarding akan menampilkan platform yang diberi wewenang. Lihat T-67, T-71, T-73. **AMANDEMEN (Bos, 2026-09-22, setelah T-66/T-67):** (a) kewajiban **menyembunyikan kirimdev DIBATALKAN**; (b) jalur resmi tetap tersedia sebagai pilihan tetapi **dikerjakan manual** — disiapkan tangan per tenant, tanpa self-service, tanpa layar pilihan di web; (c) karena manual, jalur resmi memakai **Meta Cloud API langsung**, bukan kirimdev, sehingga adaptor `whatsapp_cloud` Hermes dipakai **apa adanya** — `GRAPH_API_BASE = "https://graph.facebook.com"` justru sudah benar dan **H-04 keluar dari jalur kritis**; (d) kirimdev ditunda menjadi keputusan tersendiri di masa depan, dan saat itu pertimbangannya adalah **kenyamanan onboarding** (setup link self-service, shared inbox, multi-tenant), bukan kemampuan mengirim; (e) konsekuensi lanjutan: **T-71 tidak diperlukan untuk jalur manual** karena transport adalah urusan konfigurasi Hermes per profil dan tidak terlihat oleh kode kita — batas abstraksinya memang sudah ada di sana; T-73 ditunda karena kanal ditentukan kita, bukan dipilih tenant. |
| D-71 | **Struktur WABA B: nomor milik klien, WABA di bawah portfolio kita, pembayaran lewat kita** (Bos, 2026-09-22) | Meta menagih pemilik WABA, dan kirimdev menyatakan tagihan per pesan masuk langsung ke akun WhatsApp Business pemilik — bukan lewat invoice kirimdev. Karena itu "bayar lewat kita" **tidak otomatis**; ia menuntut WABA (atau setidaknya metode pembayarannya) berada di bawah kendali kita. Bos memilih **Struktur B**: nomor milik klien, WABA di bawah portfolio kita, metode pembayaran kita, klien tidak pernah berhadapan dengan Meta. **Ditolak Struktur A** (WABA milik klien, Meta menagih klien) karena tidak mewujudkan maksud tersebut. Tiga konsekuensi diterima sadar: (a) kita menanggung **risiko kredit** — Meta ditagih lebih dulu, klien belum tentu bayar; pengendalinya saldo prabayar + kuota + tangga D-49 (T-74, T-75), dan keduanya **wajib mendarat sebelum tenant pertama memakai jalur resmi**; (b) kita menjadi **pemroses data** percakapan pelanggan klien sehingga butuh dasar persetujuan — `companies.privacy_accepted_at` + `AiDataSharingPolicy` sudah ada sebagai pola tetapi keduanya company-scoped dan belum mencakup kasus ini; (c) **portabilitas keluar sulit** — memindahkan nomor antar WABA merepotkan dan klien yang berhenti akan merasa terkunci, jadi ini wajib dinyatakan jujur di materi penjualan. **Prasyarat yang belum terverifikasi:** apakah model multi-tenant kirimdev menempatkan WABA pelanggan di bawah portfolio kita atau di bawah portfolio pelanggan masing-masing — deskripsi publik mereka bisa berarti keduanya, dan ini harus dikonfirmasi tertulis ke kirimdev **sebelum** dijanjikan ke klien. Meter pesan wajib membedakan kategori marketing / utility / service karena tarif Meta berbeda dan sejak 1 Oktober 2026 setiap nomor mendapat 1.000 service message gratis per bulan. |

## Keputusan OPEN

D-43 dijawab Bos (palet A). Q-01..Q-08 dijawab Bos 2026-09-16 dan dipromosikan menjadi D-34..D-41
(Q-04 dijawab **berbeda dari default**: lihat D-37). Aturan tetap: autopilot **tidak boleh
menebak** item OPEN; tandai task `BLOCKED` dan lanjutkan task independen.

| ID | Pertanyaan terbuka | Muncul dari | Kenapa tidak boleh ditebak |
|---|---|---|---|
| Q-09 | **Penangkapan prospek untuk bot CS publik.** `AuthenticateMasterBot` menolak pemanggil yang belum punya company (T-17: 403 bila WA user tidak memiliki company), jadi calon pelanggan yang japri bot CS **tidak bisa** dibuatkan tiket. Apakah kita membangun jalur penangkapan prospek (lead) tersendiri, mengarahkan mereka ke pendaftaran, atau membiarkan bot hanya menjawab tanpa mencatat apa pun? | T-76 (Fase 10) | Menyimpan data orang yang belum jadi pelanggan adalah keputusan produk **dan** privasi, bukan detail teknis. |
| Q-10 | **Portfolio WABA di kirimdev.** Apakah model multi-tenant kirimdev menempatkan WABA pelanggan di bawah portfolio kita (yang D-71 butuhkan) atau di bawah portfolio pelanggan masing-masing? | D-71, T-71 | D-71 hanya bisa dijalankan bila jawabannya "portfolio kita". Harus dikonfirmasi tertulis ke kirimdev, bukan disimpulkan dari halaman pemasaran. |
| Q-11 | **Empat pekerjaan sisi Hermes (H-01..H-04): ditambal lokal atau diusulkan ke upstream?** T-66/T-67 membuktikan Hermes tidak punya endpoint kirim pesan, endpoint sesi WA/QR, endpoint pairing pengguna, maupun override base URL Cloud API. Keempatnya kecil tetapi berada di repo `hermes-agent`. | T-66, T-67 (Fase 10) | Menambal berarti utang pemeliharaan setiap Hermes diperbarui; menunggu upstream berarti T-69, T-71, T-72, dan T-77 menganggur tanpa jadwal. Ini keputusan biaya-vs-waktu milik Bos, bukan pilihan teknis agent. |
| ~~Q-12~~ | ~~**Jalur resmi: opsi A, B, atau C?**~~ **TERTUTUP** oleh amandemen D-70: jalur resmi dikerjakan **manual dengan Meta Cloud API langsung**, jadi tidak ada base URL yang perlu di-override dan tidak ada otak kedua. Opsi A/B/C hanya relevan lagi bila kirimdev diadopsi nanti. | T-67 | — |
| Q-13 | **Kapan jalur resmi berhenti boleh manual?** Struktur B (D-71) menaruh tagihan Meta di pihak kita sejak pesan pertama, sedangkan penagihan manual tidak punya penjaga teknis — hanya disiplin. Berapa tenant atau berapa rupiah per bulan yang menjadi batas sebelum T-74 (meter) dan T-75 (penagihan) wajib mendarat? | Amandemen D-70 + D-71 | Ini ambang risiko finansial, bukan pilihan teknis. Tanpa angka, "nanti" berarti sampai ada tagihan yang mengejutkan. |

Seluruh keputusan arsitektur utama telah LOCKED; dua item di atas adalah
pertanyaan operasional yang muncul dari Fase 10, bukan lubang arsitektur.

---

## Peta Dokumen PRD (`D:\PROJECTS\agentic-bos\docs\`)

1. `00-DECISIONS.md` — dokumen ini (konsolidasi semua keputusan; tie-breaker)
2. `PRD.md` — ringkasan eksekutif, visi, pilar
3. `INDUSTRY_PRESETS.md` — matriks 6 preset + feature flags + menu dinamis
4. `DATA_MODEL.md` — skema tabel canonical + kontrak migration portabel
5. `REQUIREMENTS.md` — spesifikasi fungsional per modul (termasuk formula pajak)
6. `COMMERCIAL_AND_AI_AGENTIC_SPEC.md` — membership dinamis, arsitektur 500 klien, white-label AI
7. `UX_UI_SPEC.md` — filosofi UI, onboarding, tab-based settings, dashboard adaptif, design token
8. `EXECUTION_PLAN.md` — task queue Fase 0–5, dependency graph, definisi READY
9. `PRD_RECONCILIATION.md` — log audit konflik dan resolusinya
10. `AUTOPILOT_STATUS.md` — state pekerjaan aktual untuk resume agent
